{
  "_comment": "PyPKI startup configuration. Run with: python pypki.py",
  "host": "0.0.0.0",
  "port": 8080,
  "ca_dir": "./ca",
  "log_level": "INFO",

  "cmp": {
    "prefix": "/cmp",
    "comment": "CMPv2/v3 main handler — always started, cannot be disabled here"
  },

  "web_ui": {
    "comment": "Web dashboard — always on",
    "prefix": "/",
    "no_auth": false,
    "pam_service": "login"
  },

  "acme": {
    "enabled": false,
    "prefix": "/acme",
    "cert_days": 90,
    "short_lived_threshold_days": 7,
    "auto_approve_dns": false,
    "base_url": ""
  },

  "scep": {
    "enabled": false,
    "prefix": "/scep",
    "challenge": ""
  },

  "est": {
    "enabled": false,
    "prefix": "/est",
    "require_auth": true
  },

  "ocsp": {
    "enabled": true,
    "prefix": "/ocsp",
    "cache_seconds": 300,
    "url": ""
  },

  "ipsec": {
    "enabled": false,
    "prefix": "/ipsec"
  },

  "tls": {
    "mode": "none",
    "comment": "mode: none | tls | mtls",
    "hostname": "localhost",
    "cert": "",
    "key": "",
    "tls13_only": false,
    "reload_interval": 60
  },

  "validity": {
    "end_entity_days": 365,
    "client_cert_days": 365,
    "tls_server_days": 365,
    "ca_days": 3650
  }
}
